# excel-report-email-dispatcher
Python automation that processes recipient data, validates inputs, matches report files, and prepares email dispatch workflows with attachments. Generates formatted Excel logs and summaries with built-in logging and safe dry-run mode for reliable report distribution.
